const API_URL='http://localhost:3000'; const msg=document.getElementById('sac-msg'); const count=document.getElementById('char-count');
msg?.addEventListener('input',()=> count.textContent=`${msg.value.length} / 2000`);
document.getElementById('send-sac')?.addEventListener('click', async()=>{ const body={type:document.getElementById('sac-type').value, subject:document.getElementById('sac-subject').value, email:document.getElementById('sac-email').value, message:msg.value}; try{ const res=await fetch(`${API_URL}/sac`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}); document.getElementById('sac-feedback').textContent=res.ok?'Solicitação enviada.':'Erro ao enviar.';}catch(e){ const history=JSON.parse(localStorage.getItem('sac')||'[]'); history.push(body); localStorage.setItem('sac',JSON.stringify(history)); document.getElementById('sac-feedback').textContent='API offline. Solicitação salva localmente.'; }});